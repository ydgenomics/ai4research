# Rice-Introgression 后端依赖（vllm 环境）
# 前端（rice_reg 环境）仅需：gradio, httpx, fastapi（前端只做 UI + HTTP 转发）
fastapi>=0.120
uvicorn>=0.38
pydantic>=2
python-multipart>=0.0.9
numpy>=1.26
pandas>=2.0
pyfaidx>=0.9
torch>=2.0
transformers>=4.52
peft>=0.18
safetensors>=0.6
biopython>=1.80
httpx>=0.27