# ============================================================
# Rice-Mutation Server — 统一依赖清单
# 前后端共用同一个 Python 环境，安装此文件即可。
#
# 注：rice-mut 与 rice-reg 两个项目共用同一套环境，
#     此清单已覆盖两个项目的全部运行依赖。
# ============================================================

# ===== 前端 (Gradio) =====
gradio>=5.0.0,<6.0            # 宿主机验证版本 5.50.0; 6.x 有 breaking changes
httpx>=0.25.0

# ===== 后端 (FastAPI) =====
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ===== 深度学习推理 =====
torch>=2.1.0
safetensors>=0.4.0
transformers>=4.52.0,<5.0   # 模型 config 为 transformers 4.52.4 (Mixtral + flash_attention_2); 5.x 移除旧 API
flash-attn>=2.5.0

# ===== 生物信息学 =====
pyBigWig>=0.3.22
pyfaidx>=0.7.2

# ===== 数据处理 =====
numpy>=1.24.0
pandas>=2.0.0,<3.0          # 宿主机验证版本 2.x; 3.x 有 breaking changes

# ===== 绘图 (plot_service -> src/viewer, src/dataset 顶层引用) =====
matplotlib>=3.7.0

# ===== 配置解析 (rice-reg core/model/pipeline.py 顶层 import yaml) =====
pyyaml>=6.0

# ===== 工具 =====
python-dotenv>=1.0.0

# ===== 可选(仅训练/评估用, 服务运行不需要) =====
# scikit-learn>=1.3.0        # src/metrics.py
# scipy>=1.10.0              # src/metrics.py
